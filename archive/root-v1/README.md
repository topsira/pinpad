# root-v1 archive

This folder preserves the original root version before `v3` was promoted to the public root page.

Archived files:
- `index.html`
- `styles.css`
- `app.js`

Current public root uses the v3 board experience. The `/v2/` and `/v3/` folders remain available as versioned demos.
